export interface ItemJson {
  id: number,
  name: string,
  description: string,
  value: number,
  weight: number,
  quantity: number;
  quantifiable: boolean;
  questItem: boolean;
  actionScript: string;
}
